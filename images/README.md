# images/

This project uses an external placeholder avatar URL in `index.html`:

- `https://via.placeholder.com/300x300.png?text=Avatar`

Reason: avoiding binary image files makes diffs/code review/editing easier in environments that do not render binary files inline.
